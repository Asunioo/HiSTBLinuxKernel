name: Build Hi3798MV100 Linux6.x Kernel bin
on:
  push:
    branches: [ main, linux-6.x ]
  workflow_dispatch:

jobs:
  build-kernel-6x:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          repository: ASDHiope/HiSTBLinuxKernel
          fetch-depth: 0

      - name: Install build dependencies
        run: |
          sudo apt update -y
          sudo apt install -y build-essential gcc g++ make libncurses-dev bison flex libssl-dev libelf-dev bc git wget unzip lz4 zstd cpio kmod

      - name: Cache cross compile toolchain
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: /opt/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf
          key: ${{ runner.os }}-gcc-linaro-12.3.0-armhf

      - name: Download and install Linaro 12.3.0 toolchain (多源自动重试)
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          # 定义3个备用下载源
          SOURCES=(
            "https://releases.linaro.org/components/toolchain/binaries/12.3-2023.07/arm-linux-gnueabihf/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf.tar.xz"
            "https://github.com/linaro-toolchain/binaries/releases/download/gcc-linaro-12.3.0-2023.07/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf.tar.xz"
            "https://mirrors.tuna.tsinghua.edu.cn/linaro/releases/components/toolchain/binaries/12.3-2023.07/arm-linux-gnueabihf/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf.tar.xz"
          )
          
          # 自动重试下载，最多尝试3次每个源
          for i in "${!SOURCES[@]}"; do
            echo "尝试下载源 $((i+1)): ${SOURCES[$i]}"
            if wget --tries=3 --timeout=30 "${SOURCES[$i]}" -O gcc-linaro.tar.xz; then
              echo "下载成功！"
              tar -xf gcc-linaro.tar.xz -C /opt/
              rm gcc-linaro.tar.xz
              break
            fi
            echo "源 $((i+1)) 下载失败，尝试下一个..."
          done
          
          # 检查是否下载成功
          if [ ! -d "/opt/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf/bin" ]; then
            echo "所有下载源均失败！"
            exit 1
          fi
          
          echo "/opt/gcc-linaro-12.3.0-2023.07-x86_64_arm-linux-gnueabihf/bin" >> $GITHUB_PATH

      - name: Switch to Linux6.x LTS branch
        run: |
          git checkout linux-6.x

      - name: Generate custom defconfig
        run: |
          make hi3798mv100_defconfig
          
          # 禁用所有多媒体和显示相关模块
          scripts/config --disable CONFIG_HI_VDEC
          scripts/config --disable CONFIG_HI_GPU
          scripts/config --disable CONFIG_HI_HDMI
          scripts/config --disable CONFIG_HI_MEDIA
          scripts/config --disable CONFIG_FB
          scripts/config --disable CONFIG_DRM
          scripts/config --disable CONFIG_VIDEO
          scripts/config --disable CONFIG_INPUT_KEYBOARD
          scripts/config --disable CONFIG_INPUT_MOUSE
          scripts/config --disable CONFIG_SOUND
          
          # 启用以太网和PHY驱动
          scripts/config --enable CONFIG_NET
          scripts/config --enable CONFIG_ETHERNET
          scripts/config --enable CONFIG_HI_ETH
          scripts/config --enable CONFIG_PHYLIB
          scripts/config --enable CONFIG_REALTEK_PHY
          
          # Docker 25+ 完整依赖
          scripts/config --enable CONFIG_CGROUPS
          scripts/config --enable CONFIG_CGROUP_FREEZER
          scripts/config --enable CONFIG_CGROUP_PIDS
          scripts/config --enable CONFIG_CGROUP_DEVICE
          scripts/config --enable CONFIG_CGROUP_CPUACCT
          scripts/config --enable CONFIG_MEMCG
          scripts/config --enable CONFIG_NAMESPACES
          scripts/config --enable CONFIG_OVERLAY_FS
          scripts/config --enable CONFIG_POSIX_MQUEUE
          scripts/config --enable CONFIG_SECCOMP
          scripts/config --enable CONFIG_CAPABILITIES
          scripts/config --enable CONFIG_BPF_SYSCALL
          scripts/config --enable CONFIG_NETFILTER_XT_MATCH_ADDRTYPE
          
          # 模块支持和内核优化
          scripts/config --enable CONFIG_MODULES
          scripts/config --enable CONFIG_MODULE_UNLOAD
          scripts/config --enable CONFIG_MODVERSIONS
          scripts/config --disable CONFIG_DEBUG_INFO
          scripts/config --enable CONFIG_KERNEL_ZSTD
          
          make savedefconfig
          cp defconfig arch/arm/configs/hi3798mv100_nodisp_docker_6x_defconfig

      - name: Compile kernel and generate hi_kernel-mv100-32.bin
        run: |
          export CROSS_COMPILE=arm-linux-gnueabihf-
          export ARCH=arm
          make -j2 zImage
          
          # 使用你仓库中的mkimage工具
          [ ! -f mkimage ] && wget --tries=3 https://github.com/ASDHiope/HiSTBLinuxKernel/raw/linux-4.4.y/tools/mkimage && chmod +x mkimage
          ./mkimage -A arm -O linux -T kernel -C none -a 0x40008000 -e 0x40008000 -n "Linux6.6-MV100-CLI" -d arch/arm/boot/zImage hi_kernel-mv100-32.bin
          ls -lh hi_kernel-mv100-32.bin

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: hi_kernel-mv100-32-6x-bin
          path: hi_kernel-mv100-32.bin
          retention-days: 90

      - name: Auto release
        if: github.ref == 'refs/heads/linux-6.x'
        uses: softprops/action-gh-release@v2
        with:
          files: hi_kernel-mv100-32.bin
          tag_name: kernel-mv100-6.6-${{ github.sha }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
